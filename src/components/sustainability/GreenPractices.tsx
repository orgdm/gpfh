import type { GreenPracticesProps } from '@/types/sustainability';

const GreenPractices = ({ text }: GreenPracticesProps) => {
  return <div className='px-8'>{text}</div>;
};

export default GreenPractices;
